export enum tagTypes {
  "Specialty" = "Specialty",
  "doctor" = "doctor",
  "patient" = "patient",
  "admin" = "admin",
  "super_admin" = "super_admin",
}

export const tagTypesList = Object.values(tagTypes);
